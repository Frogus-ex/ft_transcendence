# Redirige tout le trafic HTTP (80) vers HTTPS (443) - jamais de contenu servi en clair
server {
    listen 80;
    server_name localhost;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name localhost;

    ssl_certificate     /etc/nginx/certs/selfsigned.crt;
    ssl_certificate_key /etc/nginx/certs/selfsigned.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    location / {
            return 200 "Nginx + HTTPS opérationnel. En attente du frontend.\n";
            add_header Content-Type text/plain;
        }

    # --- Frontend ---
    # Tout ce qui n'est pas explicitement routé ailleurs va vers le frontend
    # location / {
    #     proxy_pass http://frontend:5173;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;

    #     # Nécessaire pour le hot-reload en dev (Vite/webpack utilisent des websockets internes)
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    # }

    # --- Backend API ---
    # À activer une fois que le service "backend" existera dans le docker-compose.yml
    # location /api/ {
    #     proxy_pass http://backend:8000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # --- WebSocket backend (temps réel) ---
    # location /ws/ {
    #     proxy_pass http://backend:8000/;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    #     proxy_set_header Host $host;
    # }
}